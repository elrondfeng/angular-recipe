
// here is the shortcut
// this is same as define the public name and public number first, then assign them the passed in value in the constructor method

export class Ingredient {
  constructor(public name: string, public amount: number) {
  }
}

